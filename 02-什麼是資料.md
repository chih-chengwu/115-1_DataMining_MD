# 資料探勘課程：從 Data 到企業資料上雲

在開始學習「分群（Clustering）」之前，我們先思考一個最基本、卻非常重要的問題：

> **資料探勘到底是在探勘什麼？**

答案就是：**Data（資料）**。

但是，「資料」並不只是 Excel 裡面的數字，也不只是資料庫中的一筆一筆紀錄。現代企業與我們日常生活中的資料，形式非常多元。了解「什麼是資料」以及「資料存在哪裡」，是學習資料探勘、機器學習與 AI 的第一步。

---

# 一、什麼是資料 Data？

## 1. 資料其實就在我們的日常生活中

我們平常很容易把「資料」想成電腦裡面的東西，例如 Excel、資料庫或 CSV 檔案。

其實，資料的範圍遠比這個廣。

例如：

* 個人的日常札記
* 筆記本上的文字
* 公司紙本的進貨資料
* 公司紙本的出貨資料
* 員工的請假單
* 客戶填寫的表單
* Excel 中的進貨、出貨資料
* Excel 中的人事資料
* 員工打卡資料
* 客戶訂單資料
* 公司 MS SQL Server 資料庫
* 網路上的資料
* 雲端硬碟中的檔案
* 網站瀏覽紀錄
* LINE、Email 等對話紀錄
* 照片與 X 光影像
* 錄音與語音
* MP4 影片

這些都可以稱為 **Data（資料）**。

---

## 2. 資料不一定是「數字」

例如：

> 王小明，2026/08/12，09:01，上班

這是一筆資料。

我們可以把它拆成：

| 欄位 | 資料         |
| -- | ---------- |
| 姓名 | 王小明        |
| 日期 | 2026/08/12 |
| 時間 | 09:01      |
| 狀態 | 上班         |

這種資料非常適合放入資料庫或 Excel 中。

但是，如果是一張牙齒 X 光照片呢？

它也是資料。

只是它不是我們平常看到的「一欄一欄的文字與數字」，而是由大量的像素（Pixel）所組成。

同樣地：

* 一段語音 → 是資料
* 一部 MP4 影片 → 是資料
* 一段文字 → 是資料
* 一張照片 → 是資料
* 一段聊天紀錄 → 是資料

因此可以簡單理解：

> **Data 不等於數字。Data 是可以被記錄、儲存、處理與分析的資訊。**

---

## 3. 資料也可以從「紙本」逐漸變成「數位資料」

假設一家公司的進貨資料最早是：

> 老闆拿一本簿子記錄每天進貨多少。

這就是資料。

後來公司使用 Excel：

> `進貨.xlsx`

這也是資料。

公司規模再變大，開始使用 MS SQL Server：

```text
Customer
Employee
Product
Order
OrderDetail
Attendance
```

資料就被儲存在資料庫的 Table 裡。

因此，我們可以看到一個很重要的概念：

```text
紙本資料
   ↓
Excel / CSV
   ↓
資料庫
   ↓
雲端資料
   ↓
大數據
   ↓
資料分析 / 資料探勘 / AI
```

資料探勘的工作，就是從大量資料中尋找：

> **人眼不容易直接發現，但具有意義的規律、關係、群組或異常。**

例如：

* 哪些商品經常一起被購買？
* 哪些客戶具有相似的消費行為？
* 哪些學生的學習行為很相似？
* 哪些病人的醫療特徵相似？
* 哪些交易可能是異常交易？

這也是為什麼我們在學習「分群（Clustering）」之前，必須先理解：

> **我們到底有哪些 Data？Data 從哪裡來？Data 又儲存在哪裡？**

---

# 二、公司伺服器上的 MS SQL，與「雲端上的 MS SQL」有什麼不同？

這是一個非常重要的觀念。

假設一家公司原本有一台伺服器：

```text
公司內部
│
└── Server
     │
     └── MS SQL Server
          │
          ├── Employee
          ├── Customer
          ├── Product
          └── Order
```

公司員工透過公司的內部網路連接這台 SQL Server。

這就是典型的：

> **On-Premises（地端／自建機房）架構**

---

## 1. 如果改成私有雲

公司也可以建立自己的「私有雲（Private Cloud）」。

概念上可能變成：

```text
公司
│
├── Private Cloud
│      │
│      ├── Virtual Machine
│      │       └── Windows Server
│      │              └── MS SQL Server
│      │
│      └── 其他企業服務
│
└── 使用者
```

這裡要注意：

> **私有雲不是單純「把 MS SQL 安裝到另一台電腦」。**

真正的私有雲通常還會包含虛擬化、資源管理、網路、儲存、權限、備份、高可用性等雲端化管理機制。

因此：

**一般 Server + MS SQL**

和

**Private Cloud + VM + MS SQL**

在「資料庫本身」可能沒有本質上的差異。

例如兩者都可以使用：

```text
SQL Server
Database
Table
Column
Row
SQL Query
```

最大的差異往往在於：

> **底層的 IT 基礎架構、資源管理方式、彈性與維運方式不同。**

---

## 2. 如果放到公有雲

例如公司使用 Microsoft Azure、Amazon Web Services（AWS）或 Google Cloud 等公有雲服務。

概念上可能變成：

```text
公司使用者
      │
      │ Internet / VPN
      ↓
   公有雲 Cloud
      │
      ├── Virtual Machine
      │       └── MS SQL Server
      │
      └── Managed Database
```

這時候，公司不一定需要自己購買：

* Server
* CPU
* RAM
* 硬碟
* 機房
* UPS
* 冷氣
* 網路設備

而是向雲端服務商租用資源。

---

## 3. 三種架構可以這樣理解

| 架構                   | Server 在哪裡？ | 誰負責硬體？ | MS SQL |
| -------------------- | ----------- | ------ | ------ |
| 地端 Server            | 公司          | 公司     | 公司管理   |
| 私有雲                  | 公司自己的雲端環境   | 公司     | 公司管理   |
| 公有雲 VM               | 雲端業者機房      | 雲端業者   | 公司管理   |
| 公有雲 Managed Database | 雲端業者        | 雲端業者   | 業者協助管理 |

因此最後一種又更進一步：

> **公司甚至不一定需要自己「安裝與管理 SQL Server」。**

例如使用雲端的 Managed Database 服務，雲端業者可以協助處理部分：

* 硬體
* 作業系統
* 資料庫軟體
* 備份
* 更新
* 擴充
* 高可用性

所以現在企業上雲，不一定等於：

> 「租一台雲端 Server，然後自己安裝 SQL Server。」

還有更方便的方式：

> **直接使用雲端資料庫服務。**

---

# 三、一般企業要架私有雲／公有雲，自架好？還是委外好？

這個問題沒有絕對答案，必須看企業的規模、資料敏感程度、IT 人力與預算。

---

## 1. 自己架設

如果公司自己架設 Private Cloud，大致上需要考慮：

```text
硬體
↓
Server
↓
Storage
↓
Network
↓
Virtualization
↓
Cloud Platform
↓
Operating System
↓
Database
↓
Application
↓
Backup / Security / Monitoring
```

這代表公司需要有相當的 IT 能力。

例如：

* Server 維護
* 網路管理
* 資安
* 備份
* 資料庫管理
* 虛擬機管理
* 防火牆
* 災難復原
* 系統監控

因此：

> **自架最大的問題通常不是「能不能架」，而是「架好之後誰來維護」。**

---

## 2. 委外或使用公有雲

如果企業 IT 人力不多，通常可以考慮：

> **把部分基礎設施交給專業廠商。**

例如：

```text
企業
 │
 └── 公有雲
       │
       ├── Server
       ├── Storage
       ├── Database
       └── Backup
```

企業主要負責：

```text
自己的資料
+
自己的應用程式
+
使用者權限
+
企業業務邏輯
```

而底層的部分基礎設施由雲端服務商負責。

---

## 3. 因此企業通常不是單純「自架 vs 委外」

實際上可以有很多種選擇：

```text
                    IT 管理程度
                         ↑
                         │
自架 Server              │
     ↓                   │
Private Cloud            │
     ↓                   │
Cloud VM                 │
     ↓                   │
Managed Database         │
     ↓                   │
SaaS                     │
                         └────────→
                           越來越少自己管理
```

因此對很多中小企業而言：

> **不一定需要自己架 Private Cloud。**

如果公司的主要需求只是：

> 「我要讓員工使用 Email、檔案、資料庫與企業應用程式。」

那麼直接使用成熟的雲端服務，可能比自己建一套完整 Private Cloud 更經濟。

---

# 四、公司資料上雲，一定要架私有雲／公有雲嗎？

答案是：

# **不一定！**

這是非常重要的觀念。

很多初學者會把：

> 「資料上雲」

理解成：

> 「我要先架一個 Cloud。」

其實不需要。

---

## 1. 最簡單的例子：雲端硬碟

例如公司使用企業版 Google Workspace。

公司可以使用：

* Gmail
* Google Drive
* Google Docs
* Google Sheets
* Google Meet

員工可以直接把：

```text
Excel
Word
PDF
圖片
影片
CSV
```

放到 Google Drive。

這些資料已經是在「雲端」了。

公司根本不需要自己建立一個：

```text
Private Cloud Server
```

才能做到。

---

## 2. 但是 Google Drive 與 SQL Server 是不同東西

這裡非常容易混淆。

假設公司有：

```text
員工.xlsx
客戶.xlsx
產品.xlsx
訂單.xlsx
```

這些檔案可以直接放到雲端硬碟。

但是如果公司原本有：

```text
SQL Server
   │
   ├── Employee Table
   ├── Customer Table
   ├── Product Table
   └── Order Table
```

那麼：

> **Google Drive 並不是拿來直接取代 SQL Server 的資料庫服務。**

你不能簡單理解成：

> 「我買 Google Workspace，再把 SQL Server 安裝到 Google Drive 裡。」

這是不行的。

---

## 3. 那如果公司真的需要 SQL Server 怎麼辦？

這時候就有不同選擇。

### 方法 A：雲端虛擬機

例如：

```text
Google Cloud
     │
     └── Virtual Machine
             │
             ├── Windows Server
             │
             └── SQL Server
```

這種方式比較接近：

> 「以前公司自己有一台 Server，現在把 Server 搬到雲端。」

---

### 方法 B：直接使用雲端資料庫服務

更進一步，公司可以使用 Cloud Provider 提供的：

> **Managed Database（託管式資料庫）**

例如使用支援 SQL Server 的雲端資料庫服務。

概念變成：

```text
公司程式
    │
    ↓
Cloud Database
    │
    └── SQL Server
```

企業不需要自己管理整台 Server。

這種方式的概念非常值得同學記住：

> **不是「把 Server 搬到雲端」而已，而是「直接使用雲端提供的資料庫服務」。**

---

# 五、最後用一個例子把四個問題串起來

假設今天有一家「ABC 公司」。

一開始：

```text
紙本進貨單
     ↓
員工輸入 Excel
     ↓
進貨.xlsx
```

公司慢慢成長：

```text
Excel
     ↓
MS SQL Server
     ↓
Employee
Customer
Product
Order
Attendance
```

再進一步：

```text
公司 Server
     ↓
Private Cloud
     ↓
Virtual Machine
     ↓
MS SQL Server
```

或者直接：

```text
公司
 ↓
Public Cloud
 ↓
Managed SQL Database
```

而公司的文件、照片、PDF、Excel 等，也可以另外使用：

```text
Google Workspace
       ↓
Google Drive
```

因此：

> **「企業資料上雲」不是只有一條路。**

可以是：

```text
                 企業資料
                    │
        ┌───────────┼───────────┐
        ↓           ↓           ↓
      文件          檔案       結構化資料
        │           │           │
        ↓           ↓           ↓
   Google Drive   Cloud      SQL Database
                              │
                    ┌─────────┴─────────┐
                    ↓                   ↓
                  Cloud VM        Managed Database
```

---

# 六、這和「資料探勘」有什麼關係？

同學現在可能會想：

> 「老師，我們今天不是要學分群嗎？為什麼一直講 Server、Cloud、SQL？」

因為：

# **沒有資料，就沒有資料探勘。**

而資料探勘的第一個問題不是：

> 「我要使用 K-Means 還是 DBSCAN？」

而是：

> **「我的資料在哪裡？」**

例如：

```text
Excel
SQL Server
CSV
Cloud Database
Google Drive
網站
文字
圖片
語音
影片
```

資料可能存在不同地方、不同格式。

資料探勘的工作，就是將這些資料取得、整理、清理、轉換，最後才能進行分析。

例如今天要做：

> **「客戶分群」**

我們可能從 SQL Server 取得：

```text
CustomerID
Age
Gender
AnnualIncome
PurchaseFrequency
TotalAmount
LastPurchaseDate
```

最後形成：

```text
客戶 A → [25, 1, 500000, 12, 85000]
客戶 B → [52, 0, 900000,  3, 120000]
客戶 C → [28, 1, 450000, 15, 92000]
...
```

這些資料經過前處理之後，就可以交給：

> **K-Means、Hierarchical Clustering、DBSCAN**

等分群演算法。

所以我們可以把整個過程想成：

```text
Data
 ↓
資料儲存
 ↓
資料庫 / Excel / Cloud
 ↓
資料取得
 ↓
資料清理
 ↓
特徵選擇 / 特徵工程
 ↓
資料探勘
 ↓
Clustering
 ↓
發現資料中的群組與規律
```

---

# 七、同學一定要記住的三個觀念

### 觀念一：Data 不等於 Database

> **Data 是資料本身；Database 是用來有組織地儲存與管理資料的系統。**

---

### 觀念二：Cloud 不等於 Database

> **Cloud 是一種提供運算、儲存、網路與各種 IT 服務的環境；Database 只是其中的一種服務。**

所以：

```text
Cloud
 ├── Virtual Machine
 ├── Storage
 ├── Network
 ├── Database
 ├── AI Service
 └── 其他服務
```

---

### 觀念三：資料上雲不等於一定要「自己架雲」

可以直接使用：

```text
Cloud Storage
Cloud File Service
SaaS
Cloud Database
Managed Database
Cloud VM
```

因此現代企業真正重要的問題不是：

> **「我要不要架一朵雲？」**

而是：

> **「我的資料是什麼？資料放在哪裡？誰需要使用？需要什麼樣的安全性、效能、可靠性與管理方式？」**

當我們把這些問題想清楚之後，再來學習資料探勘，就會比較容易理解：

> **資料探勘不是單純「套一個演算法」，而是從資料的產生、儲存、取得、整理，到最後找出有價值知識的一整個過程。**
